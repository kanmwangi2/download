
export default function SelectCompanyPage() {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center p-4">
      <div className="text-center">
        <h1 className="text-2xl font-bold mb-4">Select Company Page</h1>
        <p>This page loads without any Supabase calls or context providers.</p>
        <p>If you still see the stack overflow, the issue is elsewhere.</p>
      </div>
    </div>
  );
}
